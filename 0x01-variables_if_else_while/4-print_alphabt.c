#include <stdio.h>
/**
 * main - Entry point
 * Description - program prints all alphabets except q and e
 * Return: Always 0 (success)
 */
int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
	{

	while (ch != = 'q' && ch != = 'e')

	{
		putchar(ch);
	}
	}
	putchar('\n');

	return (0);
}

