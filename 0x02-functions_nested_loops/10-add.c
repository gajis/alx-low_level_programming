#include "main.h"
/**
 * add - Function that adds two integers and returns the result
 * @int: integer
 * Return: integer
 */

int add(int a, intb)
{
	int a;
	int b;
	int num;

	num = a + b;

	return (num);
}
