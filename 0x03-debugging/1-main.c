#include <stdio.h>

/**
 * main - cause non stop loop
 * Description: comment out the loop
 * Return: 0
 */

int main(void)
{
	int i;

	/*
	printf("Infinite loop incoming :(\n");
	i = 0;
	* while (i < 10)
	* {
	*	putchar(i);
	* }
	*/
printf("Infinite loop avoided! \\o/\n");

return (0);
}
