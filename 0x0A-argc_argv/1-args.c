#include "main.h"
#include <stdlib.h>

/**
 * _counter - Function that prints number of arguments passed into it
 * @argc: counter
 * @argv: pointer
 * Return: 0
 */
int print_num(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
